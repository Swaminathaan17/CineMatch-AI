fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pandas==2.2.2
scikit-learn==1.5.1
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
pyyaml==6.0.2

# Optional - enables real semantic mood search (ml_engine/nl_query_parser.py).
# Without it, the AI Discovery feature still works via a TF-IDF fallback,
# just with keyword-level rather than true semantic matching.
sentence-transformers==3.1.1
